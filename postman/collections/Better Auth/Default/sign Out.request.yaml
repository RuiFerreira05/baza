$kind: http-request
description: Sign out the current user
url: "{{baseUrl}}/sign-out"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: "{}"
  $type: |-
    {
      "type": "object",
      "properties": {}
    }
examples: ./.resources/sign Out.resources/examples
order: 6000
