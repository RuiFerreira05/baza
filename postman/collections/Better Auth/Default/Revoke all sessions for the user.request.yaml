$kind: http-request
description: Revoke all sessions for the user
url: "{{baseUrl}}/revoke-sessions"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: "{}"
  $type: |-
    {
      "type": "object",
      "properties": {}
    }
examples: ./.resources/Revoke all sessions for the user.resources/examples
order: 22000
