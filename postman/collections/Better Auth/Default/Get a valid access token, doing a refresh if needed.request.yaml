$kind: http-request
description: Get a valid access token, doing a refresh if needed
url: "{{baseUrl}}/get-access-token"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "providerId": "string",
      "accountId": "string",
      "userId": null
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "providerId": {
          "type": "string",
          "description": "The provider ID for the OAuth provider"
        },
        "accountId": {
          "type": [
            "string",
            "null"
          ],
          "description": "The account ID associated with the refresh token"
        },
        "userId": {
          "type": [
            "string",
            "null"
          ],
          "description": "The user ID associated with the account"
        }
      },
      "required": [
        "providerId"
      ]
    }
examples: ./.resources/Get a valid access token, doing a refresh if needed.resources/examples
order: 29000
