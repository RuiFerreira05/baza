$kind: http-request
description: Refresh the access token using a refresh token
url: "{{baseUrl}}/refresh-token"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "providerId": "string",
      "accountId": "string",
      "userId": null
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "providerId": {
          "type": "string",
          "description": "The provider ID for the OAuth provider"
        },
        "accountId": {
          "type": [
            "string",
            "null"
          ],
          "description": "The account ID associated with the refresh token"
        },
        "userId": {
          "type": [
            "string",
            "null"
          ],
          "description": "The user ID associated with the account"
        }
      },
      "required": [
        "providerId"
      ]
    }
examples: ./.resources/Refresh the access token using a refresh token.resources/examples
order: 28000
