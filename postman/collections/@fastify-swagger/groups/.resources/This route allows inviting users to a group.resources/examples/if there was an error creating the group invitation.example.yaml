$kind: http-example
request:
  url: "{{baseUrl}}/v1/restricted/groups/:id/group-members"
  method: POST
  headers:
    Content-Type: application/json
    Accept: "*/*"
  pathVariables:
    id: 535610ed-fcb7-e4e0-9c74-008a555da58a
  body:
    type: json
    content: |-
      {
        "username": "string"
      }
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: "*/*"
  body:
    type: text
    content: |-
      {
        "status": "ERROR",
        "error": {
          "type": "ResourceCreationError",
          "message": "string"
        }
      }
    $type: |-
      {
        "description": "if there was an error creating the group invitation",
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "ERROR"
            ]
          },
          "error": {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "enum": [
                  "ResourceCreationError"
                ]
              },
              "message": {
                "type": "string",
                "description": "A human readable error message"
              }
            },
            "required": [
              "type",
              "message"
            ]
          }
        },
        "required": [
          "status",
          "error"
        ]
      }
order: 3000
