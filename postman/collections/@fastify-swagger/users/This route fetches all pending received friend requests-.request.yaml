$kind: http-request
name: This route fetches all pending received friend requests.
description: This route fetches all pending received friend requests.
url: "{{baseUrl}}/v1/restricted/users/:username/friends/requests"
method: GET
headers:
  Accept: "*/*"
pathVariables:
  - key: username
    value: string
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
examples: ./.resources/This route fetches all pending received friend requests-.resources/examples
order: 18000
