$kind: http-request
name: This route fetches all pending outgoing friend requests sent by the user.
description: This route fetches all pending outgoing friend requests sent by the user.
url: "{{baseUrl}}/v1/restricted/users/:username/friends/requests/sent"
method: GET
headers:
  Accept: "*/*"
pathVariables:
  - key: username
    value: string
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
examples: ./.resources/This route fetches all pending outgoing friend requests sent by.resources/examples
order: 19000
