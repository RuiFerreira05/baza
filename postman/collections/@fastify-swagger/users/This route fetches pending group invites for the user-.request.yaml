$kind: http-request
name: This route fetches pending group invites for the user.
description: This route fetches pending group invites for the user.
url: "{{baseUrl}}/v1/restricted/users/:username/groups/invites"
method: GET
headers:
  Accept: "*/*"
pathVariables:
  - key: username
    value: string
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
examples: ./.resources/This route fetches pending group invites for the user-.resources/examples
order: 10000
