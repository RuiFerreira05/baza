$kind: http-example
request:
  url: "{{baseUrl}}/v1/restricted/users/:username/events/:idEvent"
  method: GET
  headers:
    Accept: "*/*"
  pathVariables:
    username: string
    idEvent: 535610ed-fcb7-e4e0-9c74-008a555da58a
  body:
    type: text
    content: ""
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: "*/*"
  body:
    type: text
    content: |-
      {
        "status": "ERROR",
        "error": {
          "type": "ConversionError",
          "message": "string"
        }
      }
    $type: |-
      {
        "description": "Error",
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "ERROR"
            ]
          },
          "error": {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "enum": [
                  "ConversionError"
                ]
              },
              "message": {
                "type": "string",
                "description": "A human readable error message"
              }
            },
            "required": [
              "type",
              "message"
            ]
          }
        },
        "required": [
          "status",
          "error"
        ]
      }
order: 3000
