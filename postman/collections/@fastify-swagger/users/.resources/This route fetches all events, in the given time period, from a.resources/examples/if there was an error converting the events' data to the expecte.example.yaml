$kind: http-example
name: if there was an error converting the events' data to the expected format before sending the response
request:
  url: "{{baseUrl}}/v1/restricted/users/:username/events?startDate=1962-03-27&endDate=1962-03-27"
  method: GET
  headers:
    Accept: "*/*"
  queryParams:
    - key: startDate
      value: 1962-03-27
      description: Starting from this date, the user events will be returned.
    - key: endDate
      value: 1962-03-27
      description: Until this date, the user events will be returned.
  pathVariables:
    username: string
  body:
    type: text
    content: ""
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: "*/*"
  body:
    type: text
    content: |-
      {
        "status": "ERROR",
        "error": {
          "type": "ConversionError",
          "message": "string"
        }
      }
    $type: |-
      {
        "description": "if there was an error converting the events' data to the expected format before sending the response",
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "ERROR"
            ]
          },
          "error": {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "enum": [
                  "ConversionError"
                ]
              },
              "message": {
                "type": "string",
                "description": "A human readable error message"
              }
            },
            "required": [
              "type",
              "message"
            ]
          }
        },
        "required": [
          "status",
          "error"
        ]
      }
order: 3000
