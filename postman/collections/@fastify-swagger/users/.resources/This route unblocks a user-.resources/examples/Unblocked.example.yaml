$kind: http-example
request:
  url: "{{baseUrl}}/v1/restricted/users/:username/blocks/:blockedUsername"
  method: DELETE
  headers:
    Accept: "*/*"
  pathVariables:
    username: string
    blockedUsername: string
  body:
    type: text
    content: ""
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: "*/*"
  body:
    type: text
    content: |-
      {
        "status": "OK",
        "data": ""
      }
    $type: |-
      {
        "description": "Unblocked",
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "OK"
            ]
          }
        },
        "required": [
          "status",
          "data"
        ]
      }
order: 1000
