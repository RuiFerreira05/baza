$kind: http-example
request:
  url: "{{baseUrl}}/v1/restricted/users/:username/settings"
  method: PATCH
  headers:
    Content-Type: application/json
    Accept: "*/*"
  pathVariables:
    username: string
  body:
    type: json
    content: ""
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: "*/*"
  body:
    type: text
    content: |-
      {
        "status": "OK",
        "data": ""
      }
    $type: |-
      {
        "description": "Updated",
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "OK"
            ]
          }
        },
        "required": [
          "status",
          "data"
        ]
      }
order: 1000
